{% extends 'partials/base.html.twig' %}


{% block content %}
    <div class="container">
        <h1 class="page-header">{{ page.header.title }}</h1>
        
        {% if page.content %}
        <div class="field-body">
            {{ page.content }}
        </div>
        
        {% endif %}
    </div>
{% endblock %}